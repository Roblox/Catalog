<roblox xmlns:xmime="http://www.w3.org/2005/05/xmlmime" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="http://www.roblox.com/roblox.xsd" version="4">
	<External>null</External>
	<External>nil</External>
	<Item class="Tool" referent="RBX983A948523BC4EB58D4B09377BE40D01">
		<Properties>
			<bool name="CanBeDropped">true</bool>
			<bool name="Enabled">true</bool>
			<CoordinateFrame name="Grip">
				<X>0</X>
				<Y>-0.200000003</Y>
				<Z>0.899999976</Z>
				<R00>1</R00>
				<R01>0</R01>
				<R02>0</R02>
				<R10>0</R10>
				<R11>1</R11>
				<R12>0</R12>
				<R20>0</R20>
				<R21>0</R21>
				<R22>1</R22>
			</CoordinateFrame>
			<bool name="ManualActivationOnly">false</bool>
			<string name="Name">SnowFlintlock</string>
			<bool name="RequiresHandle">true</bool>
			<Content name="TextureId"><url>http://www.roblox.com/asset/?id=325517436</url></Content>
			<string name="ToolTip"></string>
		</Properties>
		<Item class="Part" referent="RBXB597F207CF0841ECA2328A6A7B65CF76">
			<Properties>
				<bool name="Anchored">false</bool>
				<float name="BackParamA">-0.5</float>
				<float name="BackParamB">0.5</float>
				<token name="BackSurface">0</token>
				<token name="BackSurfaceInput">0</token>
				<float name="BottomParamA">-0.5</float>
				<float name="BottomParamB">0.5</float>
				<token name="BottomSurface">0</token>
				<token name="BottomSurfaceInput">0</token>
				<int name="BrickColor">194</int>
				<CoordinateFrame name="CFrame">
					<X>-0.00124000001</X>
					<Y>17.3252087</Y>
					<Z>22.0165443</Z>
					<R00>0.419762522</R00>
					<R01>0.373322785</R01>
					<R02>0.827302694</R02>
					<R10>-0.067504257</R10>
					<R11>0.92181021</R11>
					<R12>-0.381718844</R12>
					<R20>-0.905120254</R20>
					<R21>0.10438481</R21>
					<R22>0.412142307</R22>
				</CoordinateFrame>
				<bool name="CanCollide">true</bool>
				<PhysicalProperties name="CustomPhysicalProperties">
					<CustomPhysics>false</CustomPhysics>
				</PhysicalProperties>
				<float name="Elasticity">0.5</float>
				<float name="Friction">0.300000012</float>
				<float name="FrontParamA">-0.5</float>
				<float name="FrontParamB">0.5</float>
				<token name="FrontSurface">0</token>
				<token name="FrontSurfaceInput">0</token>
				<float name="LeftParamA">-0.5</float>
				<float name="LeftParamB">0.5</float>
				<token name="LeftSurface">0</token>
				<token name="LeftSurfaceInput">0</token>
				<bool name="Locked">true</bool>
				<token name="Material">256</token>
				<string name="Name">Handle</string>
				<float name="Reflectance">0</float>
				<float name="RightParamA">-0.5</float>
				<float name="RightParamB">0.5</float>
				<token name="RightSurface">0</token>
				<token name="RightSurfaceInput">0</token>
				<Vector3 name="RotVelocity">
					<X>0</X>
					<Y>0</Y>
					<Z>0</Z>
				</Vector3>
				<float name="TopParamA">-0.5</float>
				<float name="TopParamB">0.5</float>
				<token name="TopSurface">0</token>
				<token name="TopSurfaceInput">0</token>
				<float name="Transparency">0</float>
				<Vector3 name="Velocity">
					<X>0</X>
					<Y>0</Y>
					<Z>0</Z>
				</Vector3>
				<token name="formFactorRaw">3</token>
				<token name="shape">1</token>
				<Vector3 name="size">
					<X>0.5</X>
					<Y>1</Y>
					<Z>2.375</Z>
				</Vector3>
			</Properties>
			<Item class="SpecialMesh" referent="RBX83F81B6DD2374E7FA72CF262DAB52B22">
				<Properties>
					<token name="LODX">2</token>
					<token name="LODY">2</token>
					<Content name="MeshId"><url>http://www.roblox.com/asset/?id=325517338</url></Content>
					<token name="MeshType">5</token>
					<string name="Name">Mesh</string>
					<Vector3 name="Offset">
						<X>0</X>
						<Y>0</Y>
						<Z>0</Z>
					</Vector3>
					<Vector3 name="Scale">
						<X>1.5</X>
						<Y>1.5</Y>
						<Z>1.5</Z>
					</Vector3>
					<Content name="TextureId"><url>http://www.roblox.com/asset/?id=325517393</url></Content>
					<Vector3 name="VertexColor">
						<X>1</X>
						<Y>1</Y>
						<Z>1</Z>
					</Vector3>
				</Properties>
			</Item>
			<Item class="Sound" referent="RBX050434C666244AD78A2DC7AE8EA84847">
				<Properties>
					<bool name="Looped">false</bool>
					<float name="MaxDistance">0</float>
					<float name="MinDistance">0</float>
					<string name="Name">Fire</string>
					<float name="Pitch">1.5</float>
					<bool name="PlayOnRemove">false</bool>
					<Content name="SoundId"><url>http://www.roblox.com/asset/?id=16211041</url></Content>
					<float name="Volume">0.5</float>
				</Properties>
			</Item>
		</Item>
		<Item class="LocalScript" referent="RBXF45D99D45EDF49F486F1A6C43F6A1276">
			<Properties>
				<bool name="Disabled">false</bool>
				<Content name="LinkedSource"><null></null></Content>
				<string name="Name">LocalScript</string>
				<ProtectedString name="Source"><![CDATA[--Made by Luckymaxer

Tool = script.Parent
Handle = Tool:WaitForChild("Handle")

Players = game:GetService("Players")

Animations = {}

ServerControl = Tool:WaitForChild("ServerControl")
ClientControl = Tool:WaitForChild("ClientControl")

Rate = (1 / 60)

ToolEquipped = false

function SetAnimation(mode, value)
	if mode == "PlayAnimation" and value and ToolEquipped and Humanoid then
		for i, v in pairs(Animations) do
			if v.Animation == value.Animation then
				v.AnimationTrack:Stop()
				table.remove(Animations, i)
			end
		end
		local AnimationTrack = Humanoid:LoadAnimation(value.Animation)
		table.insert(Animations, {Animation = value.Animation, AnimationTrack = AnimationTrack})
		AnimationTrack:Play(value.FadeTime, value.Weight, value.Speed)
	elseif mode == "StopAnimation" and value then
		for i, v in pairs(Animations) do
			if v.Animation == value.Animation then
				v.AnimationTrack:Stop(value.FadeTime)
				table.remove(Animations, i)
			end
		end
	end
end

function CheckIfAlive()
	return (((Character and Character.Parent and Humanoid and Humanoid.Parent and Humanoid.Health > 0 and Player and Player.Parent) and true) or false)
end

function Equipped(Mouse)
	Character = Tool.Parent
	Player = Players:GetPlayerFromCharacter(Character)
	Humanoid = Character:FindFirstChild("Humanoid")
	ToolEquipped = true
	if not CheckIfAlive() then
		return
	end
	Spawn(function()
		PlayerMouse = Mouse
		Mouse.Button1Down:connect(function()
			InvokeServer("Button1Click", {Down = true})
		end)
		Mouse.Button1Up:connect(function()
			InvokeServer("Button1Click", {Down = false})
		end)
		Mouse.KeyDown:connect(function(Key)
			InvokeServer("KeyPress", {Key = Key, Down = true})
		end)
		Mouse.KeyUp:connect(function(Key)
			InvokeServer("KeyPress", {Key = Key, Down = false})
		end)
	end)
end

function Unequipped()
	for i, v in pairs(Animations) do
		if v and v.AnimationTrack then
			v.AnimationTrack:Stop()
		end
	end
	Animations = {}
	ToolEquipped = false
end

function InvokeServer(mode, value)
	local ServerReturn = nil
	pcall(function()
		ServerReturn = ServerControl:InvokeServer(mode, value)
	end)
	return ServerReturn
end

function OnClientInvoke(mode, value)
	if mode == "PlayAnimation" and value and ToolEquipped and Humanoid then
		SetAnimation("PlayAnimation", value)
	elseif mode == "StopAnimation" and value then
		SetAnimation("StopAnimation", value)
	elseif mode == "PlaySound" and value then
		value:Play()
	elseif mode == "StopSound" and value then
		value:Stop()
	elseif mode == "MousePosition" then
		return ((PlayerMouse and {Position = PlayerMouse.Hit.p, Target = PlayerMouse.Target}) or nil)
	end
end

ClientControl.OnClientInvoke = OnClientInvoke

Tool.Equipped:connect(Equipped)
Tool.Unequipped:connect(Unequipped)]]></ProtectedString>
			</Properties>
		</Item>
		<Item class="LocalScript" referent="RBX696926E38C124DEEBCC09AADEA87788C">
			<Properties>
				<bool name="Disabled">false</bool>
				<Content name="LinkedSource"><null></null></Content>
				<string name="Name">MouseIcon</string>
				<ProtectedString name="Source"><![CDATA[--Made by Luckymaxer

Mouse_Icon = "rbxasset://textures/GunCursor.png"
Reloading_Icon = "rbxasset://textures/GunWaitCursor.png"

Tool = script.Parent

Mouse = nil

function UpdateIcon()
	if Mouse then
		Mouse.Icon = Tool.Enabled and Mouse_Icon or Reloading_Icon
	end
end

function OnEquipped(ToolMouse)
	Mouse = ToolMouse
	UpdateIcon()
end

function OnChanged(Property)
	if Property == "Enabled" then
		UpdateIcon()
	end
end

Tool.Equipped:connect(OnEquipped)
Tool.Changed:connect(OnChanged)]]></ProtectedString>
			</Properties>
		</Item>
		<Item class="Camera" referent="RBX2A159D38184E4B51B6B31C95F3C0FEAD">
			<Properties>
				<Ref name="CameraSubject">null</Ref>
				<token name="CameraType">0</token>
				<CoordinateFrame name="CoordinateFrame">
					<X>-9.45215988</X>
					<Y>21.5209274</Y>
					<Z>25.92136</Z>
					<R00>0.3934834</R00>
					<R01>0.346430868</R01>
					<R02>-0.851561129</R02>
					<R10>-0</R10>
					<R11>0.926282763</R11>
					<R12>0.376829028</R12>
					<R20>0.919331729</R20>
					<R21>-0.148275971</R21>
					<R22>0.36447686</R22>
				</CoordinateFrame>
				<float name="FieldOfView">10</float>
				<CoordinateFrame name="Focus">
					<X>-0.168477774</X>
					<Y>17.4127541</Y>
					<Z>21.9478493</Z>
					<R00>1</R00>
					<R01>0</R01>
					<R02>0</R02>
					<R10>0</R10>
					<R11>1</R11>
					<R12>0</R12>
					<R20>0</R20>
					<R21>0</R21>
					<R22>1</R22>
				</CoordinateFrame>
				<string name="Name">ThumbnailCamera</string>
			</Properties>
		</Item>
		<Item class="Script" referent="RBXDD57DC8CB90742F1BC9D01EB619FC100">
			<Properties>
				<bool name="Disabled">false</bool>
				<Content name="LinkedSource"><null></null></Content>
				<string name="Name">Script</string>
				<ProtectedString name="Source"><![CDATA[--Made by Luckymaxer

Tool = script.Parent
Handle = Tool:WaitForChild("Handle")

Players = game:GetService("Players")
Debris = game:GetService("Debris")

RbxUtility = LoadLibrary("RbxUtility")
Create = RbxUtility.Create

HitScript = script:WaitForChild("HitScript")

Sparks = script:WaitForChild("Sparks")

BaseUrl = "http://www.roblox.com/asset/?id="

Grips = {
	Up = CFrame.new(0, -0.2, 0.9, 1, 0, 0, 0, 1, 0, 0, 0, 1),
	Out = CFrame.new(0, -0.2, 0.9, 1, 0, -0, 0, 1, 0.15, 0, -0.15, 1),
}

Sounds = {
	Fire = Handle:WaitForChild("Fire"),
}

BasePart = Create("Part"){
	Material = Enum.Material.Plastic,
	Shape = Enum.PartType.Block,
	TopSurface = Enum.SurfaceType.SmoothNoOutlines,
	BottomSurface = Enum.SurfaceType.SmoothNoOutlines,
	FrontSurface = Enum.SurfaceType.SmoothNoOutlines,
	BackSurface = Enum.SurfaceType.SmoothNoOutlines,
	LeftSurface = Enum.SurfaceType.SmoothNoOutlines,
	RightSurface = Enum.SurfaceType.SmoothNoOutlines,
	FormFactor = Enum.FormFactor.Custom,
	Size = Vector3.new(0.2, 0.2, 0.2),
	CanCollide = true,
	Locked = true,
}

BaseSnowball = BasePart:Clone()
BaseSnowball.Name = "Projectile"
BaseSnowball.BrickColor = BrickColor.new("Institutional white")
BaseSnowball.Material = Enum.Material.Concrete
BaseSnowball.Shape = Enum.PartType.Block
BaseSnowball.Size = Vector3.new(0.75, 0.75, 0.75)
BaseSnowball.CanCollide = true
Create("SpecialMesh"){
	Name = "Mesh",
	MeshType = Enum.MeshType.Sphere,
	MeshId = "",
	TextureId = "",
	Scale = Vector3.new(1, 1, 1),
	VertexColor = Vector3.new(1, 1, 1),
	Offset = Vector3.new(0, 0, 0),
	Parent = BaseSnowball,
}

Gravity = 196.20

Force = 110
Damage = 8
FireRate = 0.5

ToolEquipped = false

ServerControl = (Tool:FindFirstChild("ServerControl") or Create("RemoteFunction"){
	Name = "ServerControl",
	Parent = Tool,
})

ClientControl = (Tool:FindFirstChild("ClientControl") or Create("RemoteFunction"){
	Name = "ClientControl",
	Parent = Tool,
})

Handle.Transparency = 0
Tool.Grip = Grips.Up
Tool.Enabled = true

function Fire(StartPos, TargetPos)
	local Snowball = BaseSnowball:Clone()
	local Mass = (Snowball:GetMass() * Gravity)
	local Direction = CFrame.new(StartPos, TargetPos)
	local Orientation = (Direction * CFrame.Angles((math.pi / 2), 0, 0))
	local Velocity = (Direction.lookVector * Force)
	local BodyVelocity = Create("BodyVelocity"){
		maxForce = Vector3.new((Mass * 0.65), (Mass * 0.85), (Mass * 0.65)),
		velocity = Velocity,
		Parent = Snowball,
	}
	Snowball.Velocity = Velocity
	local BodyGyro = Create("BodyGyro"){
		maxTorque = Vector3.new(math.huge, math.huge, math.huge),
		cframe = Orientation
	}
	local Values = {
		{Name = "Creator", Class = "ObjectValue", Value = Player},
		{Name = "Damage", Class = "NumberValue", Value = Damage},
	}
	local SparksCopy = Sparks:Clone()
	SparksCopy.Color = ColorSequence.new(Color3.new(1, 1, 1), Color3.new(1, 1, 1))
	SparksCopy.Enabled = true
	SparksCopy.Parent = Snowball
	local HitScriptCopy = HitScript:Clone()
	for i, v in pairs(Values) do
		local Value = Create(v.Class){
			Name = v.Name,
			Value = v.Value,
			Parent = HitScriptCopy,
		}
	end
	HitScriptCopy.Disabled = false
	HitScriptCopy.Parent = Snowball
	Debris:AddItem(Snowball, 5)
	Snowball.Parent = game:GetService("Workspace")
	Snowball.CFrame = (Orientation + Direction.lookVector * ((Handle.Size.Z / 2) + (Snowball.Size.Y / 2)))
end

function CheckIfAlive()
	return (((Character and Character.Parent and Humanoid and Humanoid.Parent and Humanoid.Health > 0 and Torso and Torso.Parent and Player and Player.Parent) and true) or false)
end

function Activated()
	if not Tool.Enabled or not ToolEquipped or not CheckIfAlive() then
		return
	end
	Tool.Enabled = false
	local MousePosition = InvokeClient("MousePosition")
	if not MousePosition then
		Tool.Enabled = true
		return
	end
	Sounds.Fire:Play()
	local StartPosition = (Handle.CFrame * CFrame.new(0, (Handle.Size.Y / 2), -(Handle.Size.Z + (BaseSnowball.Size.Z / 2)))).p
	local TargetPosition = MousePosition.Position
	Tool.Grip = Grips.Out	Fire(StartPosition, TargetPosition)
	wait(FireRate)
	Tool.Grip = Grips.Up
	Tool.Enabled = true
end

function Equipped(Mouse)
	Character = Tool.Parent
	Humanoid = Character:FindFirstChild("Humanoid")
	Torso = Character:FindFirstChild("Torso")
	Player = Players:GetPlayerFromCharacter(Character)
	if not CheckIfAlive() then
		return
	end
	Tool.Grip = Grips.Up
	ToolEquipped = true
end

function Unequipped()
	Tool.Grip = Grips.Up
	ToolEquipped = false
end

function OnServerInvoke(player, mode, value)
	if player ~= Player or not ToolEquipped or not value or not CheckIfAlive() then
		return
	end
end

function InvokeClient(Mode, Value)
	local ClientReturn = nil
	pcall(function()
		ClientReturn = ClientControl:InvokeClient(Player, Mode, Value)
	end)
	return ClientReturn
end

ServerControl.OnServerInvoke = OnServerInvoke

Tool.Activated:connect(Activated)
Tool.Equipped:connect(Equipped)
Tool.Unequipped:connect(Unequipped)]]></ProtectedString>
			</Properties>
			<Item class="Script" referent="RBX16F7E700667D4BD2A24E8380ABF9C3DA">
				<Properties>
					<bool name="Disabled">true</bool>
					<Content name="LinkedSource"><null></null></Content>
					<string name="Name">HitScript</string>
					<ProtectedString name="Source"><![CDATA[--Made by Luckymaxer

Part = script.Parent

Players = game:GetService("Players")
Debris = game:GetService("Debris")

RbxUtility = LoadLibrary("RbxUtility")
Create = RbxUtility.Create

SnowMeltScript = script:WaitForChild("SnowMeltScript")

BaseUrl = "http://www.roblox.com/asset/?id="

BasePart = Create("Part"){
	Material = Enum.Material.Plastic,
	Shape = Enum.PartType.Block,
	TopSurface = Enum.SurfaceType.SmoothNoOutlines,
	BottomSurface = Enum.SurfaceType.SmoothNoOutlines,
	FrontSurface = Enum.SurfaceType.SmoothNoOutlines,
	BackSurface = Enum.SurfaceType.SmoothNoOutlines,
	LeftSurface = Enum.SurfaceType.SmoothNoOutlines,
	RightSurface = Enum.SurfaceType.SmoothNoOutlines,
	FormFactor = Enum.FormFactor.Custom,
	Size = Vector3.new(0.2, 0.2, 0.2),
	CanCollide = true,
	Locked = true,
}

Creator = script:WaitForChild("Creator")
Damage = script:WaitForChild("Damage")

AlreadyHit = false

function GetCreatorPlayer()
	return (((Creator and Creator.Value and Creator.Value.Parent and Creator.Value:IsA("Player")) and Creator.Value) or nil)
end

function IsTeamMate(Player1, Player2)
	return (Player1 and Player2 and not Player1.Neutral and not Player2.Neutral and Player1.TeamColor == Player2.TeamColor)
end

function TagHumanoid(humanoid, player)
	local Creator_Tag = Create("ObjectValue"){
		Name = "creator",
		Value = player,
	}
	Debris:AddItem(Creator_Tag, 2)
	Creator_Tag.Parent = humanoid
end

function UntagHumanoid(humanoid)
	for i, v in pairs(humanoid:GetChildren()) do
		if v:IsA("ObjectValue") and v.Name == "creator" then
			v:Destroy()
		end
	end
end

function PlaySound(Position, Sound)
	local Sound = Sound:Clone()
	local SoundPart = BasePart:Clone()
	SoundPart.Name = "Effect"
	SoundPart.Transparency = 1
	SoundPart.Size = Vector3.new(0.2, 0.2, 0.2)
	SoundPart.CanCollide = false
	SoundPart.Anchored = true
	SoundPart.CFrame = CFrame.new(Position)
	Sound.Parent = SoundPart
	Debris:AddItem(SoundPart, 1.5)
	SoundPart.Parent = game:GetService("Workspace")
	Sound:Play()
end

function SplashScreen(Player)
	if not Player or not Player:IsA("Player") then
		return
	end
	local PlayerGui = Player:FindFirstChild("PlayerGui")
	if not PlayerGui then
		return
	end
	local Flakes = {"19003948", "19003957", "19003971", "19003978", "19003982", "19003990", "19003998"}
	local Globs = {"19323823", "19323854"}
	local SplatGui = Create("ScreenGui"){
		Name = "SplatGui",
	}
	local Frame = Create("Frame") {
		Name = "SnowEffect",
		Position = UDim2.new(0, 0, 0, 0),
		Size = UDim2.new(1, 0, 1, 0),
		BackgroundTransparency = 1,
		Parent = SplatGui,
	}
	--Big Glob
	local Size = ((math.random() * 0.2) + 0.5)
	local s = Create("ImageLabel"){
		Size = UDim2.new(Size, 0, Size, 0),
		Position = UDim2.new((math.random() - (Size / 2)), 0, (math.random() - (Size / 2)), 0),
		SizeConstraint = Enum.SizeConstraint.RelativeXX,
		Rotation = math.random(0, 360),
		BackgroundTransparency = 1,
		Image = (BaseUrl .. Globs[math.random(1, #Globs)]),
		Parent = Frame,
	}
	Debris:AddItem(s, (5 + (math.random() * 2)))
	--Flakes
	for i = 1, 20 do
		local Size = ((math.random() * 0.05) + 0.05)
		local s = Create("ImageLabel"){
			Size = UDim2.new(Size, 0, Size, 0),
			Position = UDim2.new((math.random() - (Size / 2)), 0, (math.random() - (Size / 2)), 0),
			SizeConstraint = Enum.SizeConstraint.RelativeXX,
			Rotation = math.random(0, 360),
			BackgroundTransparency = 1,
			Image = (BaseUrl .. Flakes[math.random(1, #Flakes)]),
			Parent = Frame,
		}
		Debris:AddItem(s, (3 + (math.random() * 6)))
	end
	local SnowMeltScriptCopy = SnowMeltScript:Clone()
	local Delay = Create("NumberValue"){
		Name = "Delay",
		Value = 3,
		Parent = SnowMeltScriptCopy,
	}
	SnowMeltScriptCopy.Disabled = false
	SnowMeltScriptCopy.Parent = Frame
	Debris:AddItem(SplatGui, 10)
	SplatGui.Parent = PlayerGui
end


function Explode(Amount)
	for i = 1, Amount do
		local Flake = BasePart:Clone()
		Flake.Name = "Effect"
		Flake.BrickColor = Part.BrickColor
		Flake.Material = Enum.Material.Concrete
		Flake.Size = Vector3.new(0.75, 0.2, 0.75)
		Flake.CanCollide = true
		Flake.Velocity = (Part.Velocity * Vector3.new((math.random() * 0.65), (2 + (math.random() * 1)), (math.random() * 0.65)))
		Flake.RotVelocity = Vector3.new(math.random(-20, 20), math.random(-20, 20), math.random(-20, 20))
		Flake.CFrame = (Part.CFrame + (Part.Velocity * math.random() * 0.05) + Vector3.new(0, (math.random() * 3), 0))
		Debris:AddItem(Flake, (30 + (math.random() * 5)))
		Flake.Parent = game:GetService("Workspace")
	end
end

function CheckTableForString(Table, String)
	for i, v in pairs(Table) do
		if string.lower(v) == string.lower(String) then
			return true
		end
	end
	return false
end

function Blow(Hit)
	local IgnoredNames = {"Effect", "Projectile"}
	if not Hit or not Hit.Parent or CheckTableForString(IgnoredNames, Hit.Name) or AlreadyHit then
		return
	end
	local CreatorPlayer = GetCreatorPlayer()
	local character = Hit.Parent
	if character:IsA("Hat") then
		character = character.Parent
	end
	local humanoid = character:FindFirstChild("Humanoid")
	if humanoid and humanoid.Health > 0 then
		local CanDamage = true
		local player = Players:GetPlayerFromCharacter(character)
		if player and CreatorPlayer then
			if player == CreatorPlayer then
				return
			end
			if IsTeamMate(CreatorPlayer, player) then
				CanDamage = false
			end
		end
		if CanDamage then
			UntagHumanoid(humanoid)
			TagHumanoid(humanoid, CreatorPlayer)
			humanoid:TakeDamage(Damage.Value)
			if player then
				SplashScreen(player)
			end
		end
	end
	AlreadyHit = true
	Part.Anchored = true
	Part.CanCollide = false
	Part.Transparency = 1
	for i, v in pairs(Part:GetChildren()) do
		if v:IsA("ParticleEmitter") then
			v.Enabled = false
		end
	end
	local HitSounds = {"19326853", "19326880", "19326891"}
	local Sound = Create("Sound"){
		SoundId = (BaseUrl .. HitSounds[math.random(1, #HitSounds)]),
		Pitch = 1,
		Volume = 1,
		Looped = false,
		PlayOnRemove = false,
	}
	PlaySound(Part.Position, Sound)
	Explode(math.random(4, 8))
end

Part.Touched:connect(Blow)]]></ProtectedString>
				</Properties>
				<Item class="LocalScript" referent="RBX807ACA7543E24D0F857C52C6ECAF8695">
					<Properties>
						<bool name="Disabled">true</bool>
						<Content name="LinkedSource"><null></null></Content>
						<string name="Name">SnowMeltScript</string>
						<ProtectedString name="Source"><![CDATA[--Made by Luckymaxer

Frame = script.Parent
Delay = script:WaitForChild("Delay")
wait(Delay.Value)
local TimeToFade = 1
local FadeRate = 0.025
local OrigTransparency = 0
for i = 1, (TimeToFade / FadeRate) do
	wait(FadeRate)
	for ii, vv in pairs(Frame:GetChildren()) do
		if vv:IsA("GuiObject") then
			vv.ImageTransparency = (OrigTransparency + (i / (TimeToFade / FadeRate)))
			vv.Position = (vv.Position + UDim2.new(0, 0, 0, math.random(0, 4)))
			vv.Size = (vv.Size + UDim2.new(0, 0, 0, math.random(0, 2)))
		end
	end
end]]></ProtectedString>
					</Properties>
				</Item>
			</Item>
			<Item class="ParticleEmitter" referent="RBXCB97D41DC12E4A129310D9D87043556E">
				<Properties>
					<Vector3 name="Acceleration">
						<X>0</X>
						<Y>0.200000003</Y>
						<Z>0</Z>
					</Vector3>
					<ColorSequence name="Color">0 0 0 0 0 1 0.392157 0.392157 0.392157 0 </ColorSequence>
					<float name="Drag">0</float>
					<token name="EmissionDirection">2</token>
					<bool name="Enabled">false</bool>
					<NumberRange name="Lifetime">0.25 0.25 </NumberRange>
					<float name="LightEmission">0.200000003</float>
					<bool name="LockedToPart">false</bool>
					<string name="Name">Sparks</string>
					<float name="Rate">30</float>
					<NumberRange name="RotSpeed">-50 50 </NumberRange>
					<NumberRange name="Rotation">0 360 </NumberRange>
					<NumberSequence name="Size">0 0.1 0 1 0.1 0 </NumberSequence>
					<NumberRange name="Speed">-0.1 0.1 </NumberRange>
					<Content name="Texture"><url>http://www.roblox.com/asset/?id=277951744</url></Content>
					<NumberSequence name="Transparency">0 0 0 1 0 0 </NumberSequence>
					<float name="VelocityInheritance">0</float>
					<float name="VelocitySpread">25</float>
					<float name="ZOffset">0</float>
				</Properties>
			</Item>
		</Item>
	</Item>
</roblox>
<roblox xmlns:xmime="http://www.w3.org/2005/05/xmlmime" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="http://www.roblox.com/roblox.xsd" version="4">
	<External>null</External>
	<External>nil</External>
	<Item class="Tool" referent="RBX264BF77D57FD409183C2D4DF2B9A5AC9">
		<Properties>
			<bool name="CanBeDropped">true</bool>
			<bool name="Enabled">true</bool>
			<CoordinateFrame name="Grip">
				<X>0</X>
				<Y>0</Y>
				<Z>1.75</Z>
				<R00>1</R00>
				<R01>0</R01>
				<R02>0</R02>
				<R10>-0</R10>
				<R11>8.10044585e-005</R11>
				<R12>1</R12>
				<R20>0</R20>
				<R21>-1</R21>
				<R22>8.10044585e-005</R22>
			</CoordinateFrame>
			<bool name="ManualActivationOnly">false</bool>
			<string name="Name">WhiteGoldBlueFlowerSword</string>
			<Content name="TextureId"><url>http://www.roblox.com/asset/?id=225551299</url></Content>
			<string name="ToolTip"></string>
		</Properties>
		<Item class="Part" referent="RBX18606745B17F46C9A10EDE3283FF4662">
			<Properties>
				<bool name="Anchored">false</bool>
				<float name="BackParamA">-0.5</float>
				<float name="BackParamB">0.5</float>
				<token name="BackSurface">0</token>
				<token name="BackSurfaceInput">0</token>
				<float name="BottomParamA">-0.5</float>
				<float name="BottomParamB">0.5</float>
				<token name="BottomSurface">0</token>
				<token name="BottomSurfaceInput">0</token>
				<int name="BrickColor">194</int>
				<CoordinateFrame name="CFrame">
					<X>22.6768723</X>
					<Y>6.87944126</Y>
					<Z>-4.84310341</Z>
					<R00>1</R00>
					<R01>0</R01>
					<R02>0</R02>
					<R10>0</R10>
					<R11>0.707106709</R11>
					<R12>-0.707106769</R12>
					<R20>0</R20>
					<R21>0.707106769</R21>
					<R22>0.707106709</R22>
				</CoordinateFrame>
				<bool name="CanCollide">true</bool>
				<float name="Elasticity">0.5</float>
				<float name="Friction">0.300000012</float>
				<float name="FrontParamA">-0.5</float>
				<float name="FrontParamB">0.5</float>
				<token name="FrontSurface">0</token>
				<token name="FrontSurfaceInput">0</token>
				<float name="LeftParamA">-0.5</float>
				<float name="LeftParamB">0.5</float>
				<token name="LeftSurface">0</token>
				<token name="LeftSurfaceInput">0</token>
				<bool name="Locked">true</bool>
				<token name="Material">256</token>
				<string name="Name">Handle</string>
				<float name="Reflectance">0</float>
				<float name="RightParamA">-0.5</float>
				<float name="RightParamB">0.5</float>
				<token name="RightSurface">0</token>
				<token name="RightSurfaceInput">0</token>
				<Vector3 name="RotVelocity">
					<X>0</X>
					<Y>0</Y>
					<Z>0</Z>
				</Vector3>
				<float name="TopParamA">-0.5</float>
				<float name="TopParamB">0.5</float>
				<token name="TopSurface">0</token>
				<token name="TopSurfaceInput">0</token>
				<float name="Transparency">0</float>
				<Vector3 name="Velocity">
					<X>0</X>
					<Y>0</Y>
					<Z>0</Z>
				</Vector3>
				<token name="formFactorRaw">3</token>
				<token name="shape">1</token>
				<Vector3 name="size">
					<X>0.25</X>
					<Y>1</Y>
					<Z>5.5</Z>
				</Vector3>
			</Properties>
			<Item class="SpecialMesh" referent="RBX21A5F5212F78482783F418491B3CC6AB">
				<Properties>
					<token name="LODX">2</token>
					<token name="LODY">2</token>
					<Content name="MeshId"><url>http://www.roblox.com/asset/?id=225551249</url></Content>
					<token name="MeshType">5</token>
					<string name="Name">Mesh</string>
					<Vector3 name="Offset">
						<X>0</X>
						<Y>0</Y>
						<Z>0</Z>
					</Vector3>
					<Vector3 name="Scale">
						<X>1</X>
						<Y>1</Y>
						<Z>1</Z>
					</Vector3>
					<Content name="TextureId"><url>http://www.roblox.com/asset/?id=225551359</url></Content>
					<Vector3 name="VertexColor">
						<X>1</X>
						<Y>1</Y>
						<Z>1</Z>
					</Vector3>
				</Properties>
			</Item>
			<Item class="Sound" referent="RBX6EEB3171D32E41349511B29429081D11">
				<Properties>
					<bool name="Looped">false</bool>
					<string name="Name">Slash</string>
					<float name="Pitch">1.153</float>
					<bool name="PlayOnRemove">false</bool>
					<Content name="SoundId"><url>http://www.roblox.com/asset/?id=96667910</url></Content>
					<float name="Volume">1</float>
				</Properties>
			</Item>
			<Item class="Sound" referent="RBXD6AD165164FE455A83F95B1991F6A8FA">
				<Properties>
					<bool name="Looped">false</bool>
					<string name="Name">Unsheath</string>
					<float name="Pitch">0.99000001</float>
					<bool name="PlayOnRemove">false</bool>
					<Content name="SoundId"><url>http://www.roblox.com/asset/?id=12222225</url></Content>
					<float name="Volume">0.5</float>
				</Properties>
			</Item>
			<Item class="Sound" referent="RBXEAB597188DA1434386E1584DAD0A23D1">
				<Properties>
					<bool name="Looped">false</bool>
					<string name="Name">Flames</string>
					<float name="Pitch">1.04999995</float>
					<bool name="PlayOnRemove">false</bool>
					<Content name="SoundId"><url>http://www.roblox.com/asset/?id=163570653</url></Content>
					<float name="Volume">0.400000006</float>
				</Properties>
			</Item>
			<Item class="Sound" referent="RBX96ED2C0F5DB344D380E13DED768C4FB9">
				<Properties>
					<bool name="Looped">false</bool>
					<string name="Name">Flowers</string>
					<float name="Pitch">0.799000025</float>
					<bool name="PlayOnRemove">false</bool>
					<Content name="SoundId"><url>http://www.roblox.com/asset/?id=102545023</url></Content>
					<float name="Volume">0.400000006</float>
				</Properties>
			</Item>
			<Item class="PointLight" referent="RBX98351947D5D7427A95B4153046C0C952">
				<Properties>
					<float name="Brightness">1</float>
					<Color3 name="Color">4281588479</Color3>
					<bool name="Enabled">true</bool>
					<string name="Name">Light</string>
					<float name="Range">6</float>
					<bool name="Shadows">true</bool>
				</Properties>
			</Item>
		</Item>
		<Item class="Script" referent="RBXF20DCBE5CDBB458CBCED587B81270E1A">
			<Properties>
				<bool name="Disabled">false</bool>
				<Content name="LinkedSource"><null></null></Content>
				<string name="Name">Script</string>
				<ProtectedString name="Source"><![CDATA[--Made by Luckymaxer

Tool = script.Parent
Handle = Tool:WaitForChild("Handle")

Players = game:GetService("Players")
Debris = game:GetService("Debris")

FlowerScript = script:WaitForChild("FlowerScript")

BaseUrl = "http://www.roblox.com/asset/?id="

BasePart = Instance.new("Part")
BasePart.Shape = Enum.PartType.Block
BasePart.Material = Enum.Material.Plastic
BasePart.TopSurface = Enum.SurfaceType.Smooth
BasePart.BottomSurface = Enum.SurfaceType.Smooth
BasePart.FormFactor = Enum.FormFactor.Custom
BasePart.Size = Vector3.new(0.2, 0.2, 0.2)
BasePart.CanCollide = true
BasePart.Locked = true
BasePart.Anchored = false

Animations = {
	Equip = {Animation = Tool:WaitForChild("Equip"), FadeTime = nil, Weight = nil, Speed = nil, Duration = nil},
	Summon = {Animation = Tool:WaitForChild("Summon"), FadeTime = nil, Weight = nil, Speed = 0.75, Duration = 2},
	LeftSlash = {Animation = Tool:WaitForChild("LeftSlash"), FadeTime = nil, Weight = nil, Speed = 1.2, Duration = 0.75, Damage = {Min = 30, Max = 35}},
	RightSlash = {Animation = Tool:WaitForChild("RightSlash"), FadeTime = nil, Weight = nil, Speed = 1.19, Duration = 0.75, Damage = {Min = 30, Max = 35}},
}

Sounds = {
	Slash = Handle:WaitForChild("Slash"),
	Unsheath = Handle:WaitForChild("Unsheath"),
	Flames = Handle:WaitForChild("Flames"),
	Flowers = Handle:WaitForChild("Flowers")
}

TouchDamage = {Min = 0, Max = 0}
ToolEquipped = false
Attacking = false

SpecialEnabled = true
SpecialActive = false
SpecialDuration = 30
SpecialReloadTime = 15

FlameDuration = 0
FlamesActive = false

ReloadTime = 0.75

Gravity = 196.20
Rate = (1 / 60)

ProjectileNames = {"Water", "Arrow", "Projectile", "Effect", "Rail", "Laser", "Bullet"}

HumanoidsHit = {}

ServerControl = (Tool:FindFirstChild("ServerControl") or Instance.new("RemoteFunction"))
ServerControl.Name = "ServerControl"
ServerControl.Parent = Tool

ClientControl = (Tool:FindFirstChild("ClientControl") or Instance.new("RemoteFunction"))
ClientControl.Name = "ClientControl"
ClientControl.Parent = Tool

Tool.Enabled = true

function CheckTableForString(Table, String)
	for i, v in pairs(Table) do
		if string.lower(v) == string.lower(String) then
			return true
		end
	end
	return false
end

function CheckIntangible(Hit)
	if Hit and Hit.Parent then
		if Hit.Transparency > 0.8 or CheckTableForString(ProjectileNames, Hit.Name) or Hit.Parent:FindFirstChild("Humanoid") then
			return true
		end
	end
	return false
end

function CastRay(StartPos, Vec, Length, Ignore, DelayIfHit)
	local Ignore = ((type(Ignore) == "table" and Ignore) or {Ignore})
	local RayHit, RayPos, RayNormal = game:GetService("Workspace"):FindPartOnRayWithIgnoreList(Ray.new(StartPos, Vec * Length), Ignore)
	if RayHit and CheckIntangible(RayHit) then
		if DelayIfHit then
			wait()
		end
		RayHit, RayPos, RayNormal = CastRay((RayPos + (Vec * 0.01)), Vec, (Length - ((StartPos - RayPos).magnitude)), Ignore, DelayIfHit)
	end
	return RayHit, RayPos, RayNormal
end

function IsTeamMate(Player1, Player2)
	return (Player1 and Player2 and not Player1.Neutral and not Player2.Neutral and Player1.TeamColor == Player2.TeamColor)
end

function TagHumanoid(humanoid, player)
	local Creator_Tag = Instance.new("ObjectValue")
	Creator_Tag.Name = "creator"
	Creator_Tag.Value = player
	Debris:AddItem(Creator_Tag, 2)
	Creator_Tag.Parent = humanoid
end

function UntagHumanoid(humanoid)
	for i, v in pairs(humanoid:GetChildren()) do
		if v:IsA("ObjectValue") and v.Name == "creator" then
			v:Destroy()
		end
	end
end

function CheckTableForInstance(Table, Instance)
	for i, v in pairs(Table) do
		if v == Instance then
			return true
		end
	end
	return false
end

function CreateFirePart(Create)
	for i, v in pairs(Handle:GetChildren()) do
		if v:IsA("BasePart") and v.Name == "FirePart" then
			v:Destroy()
		end
	end
	if not Create then
		return
	end
	local FirePart = BasePart:Clone()
	FirePart.Name = "FirePart"
	FirePart.Transparency = 1
	FirePart.CanCollide = false
	local Fire = Instance.new("Fire")
	Fire.Color = Color3.new((255 / 255), (255 / 255), (0 / 255))
	Fire.SecondaryColor = Color3.new((255 / 255), (255 / 255),  (127 / 255))
	Fire.Heat = 25
	Fire.Size = 3
	Fire.Enabled = true
	Fire.Parent = FirePart
	local FireWeld = Instance.new("Weld")
	FireWeld.Part0 = Handle
	FireWeld.Part1 = FirePart
	FireWeld.C0 = (FireWeld.C0 * CFrame.new(0, 0, 0.5)) * CFrame.Angles(-(math.pi / 2), 0, 0)
	FireWeld.Parent = FirePart
	FirePart.Parent = Handle
end

function Blow(Hit)
	if not Hit or not Hit.Parent or not CheckIfAlive() then
		return
	end
	
	local RightArm = Character:FindFirstChild("Right Arm")
	if not RightArm then
		return
	end
	local RightGrip = RightArm:FindFirstChild("RightGrip")
	if not RightGrip or (RightGrip.Part0 ~= RightArm and RightGrip.Part1 ~= RightArm) then
		return
	end
	
	local character = Hit.Parent
	if character == Character then
		return
	end
	
	local player = Players:GetPlayerFromCharacter(character)
	if player and IsTeamMate(Player, player) then
		return
	end
	
	local humanoid = character:FindFirstChild("Humanoid")
	if not humanoid or humanoid.Health == 0 or CheckTableForInstance(HumanoidsHit, humanoid) then
		return
	end
	
	table.insert(HumanoidsHit, humanoid)
	
	local DamageValue = ((CurrentAnimation and CurrentAnimation.Damage) or TouchDamage)
	local DamageAmount = (math.random(DamageValue.Min, DamageValue.Max) * ((FlamesActive and 1.10) or 1))
	
	UntagHumanoid(humanoid)
	TagHumanoid(humanoid, Player)
	
	local HealthAfter = (humanoid.Health - DamageAmount)
	
	humanoid:TakeDamage(DamageAmount)
	
	if HealthAfter <= 0 then
		if not FlamesActive then
			FlamesActive = true
			Sounds.Flames:Play()
			CreateFirePart(true)
			Spawn(function()
				while FlamesActive and ToolEquipped and CheckIfAlive() and FlameDuration > 0 do
					local Time = wait(Rate)
					FlameDuration = (FlameDuration - Time)
				end
				CreateFirePart(false)
				FlameDuration = 0
				FlamesActive = false
			end)
		else
			local HealthBack = (Humanoid.MaxHealth * 0.20)
			Humanoid.Health = (Humanoid.Health + HealthBack)
		end
		FlameDuration = 5
	end
	
end

function Attack(PossibleAnims, PossibleSounds)
	if PossibleAnims and #PossibleAnims > 0 then
		if PossibleSounds and #PossibleSounds > 0 then
			local RandomSound = PossibleSounds[math.random(1, #PossibleSounds)]
			RandomSound:Play()
		end
		local SelectedAnimation = PossibleAnims[math.random(1, #PossibleAnims)]
		Delay(((SelectedAnimation.Delay and SelectedAnimation.Delay) or 0), (function()
			HumanoidsHit = {}
			CurrentAnimation = SelectedAnimation
			InvokeClient("PlayAnimation", CurrentAnimation)
			if CurrentAnimation and CurrentAnimation.Duration and CurrentAnimation.Duration > 0 then
				wait(CurrentAnimation.Duration)
			end
			CurrentAnimation = nil
			HumanoidsHit = {}
		end))
	end
end

function Activated()
	if not Tool.Enabled or not ToolEquipped then
		return
	end
	Tool.Enabled = false
	Attacking = true
	local AttackSounds = {Sounds.Slash}
	local AttackAnims = {Animations.LeftSlash, Animations.RightSlash}
	Attack(AttackAnims, AttackSounds)
	Attacking = false
	wait(ReloadTime)
	Tool.Enabled = true
end

function GrowFlower(Part, Position)
	if not Part or not Part.Parent or not Position then
		return
	end
	local ToObjectSpace = Part.CFrame:toObjectSpace(CFrame.new(Position + Vector3.new(0, -1, 0)) * CFrame.Angles(0, (math.random() * math.pi * 2), 0))
	if not ToObjectSpace then
		return
	end
	local GrowRate = 0.1
	local GrowHeight = (0.5 + math.random())
	local PlantType = math.random(1, 2)
	local Flower = BasePart:Clone()
	Flower.Name = "Effect"
	Flower.CanCollide = false
	local FlowerMesh = Instance.new("SpecialMesh")
	if PlantType == 1 then
		FlowerMesh.MeshId = (BaseUrl .. "12212520")
		FlowerMesh.TextureId = (BaseUrl .. "12212536")
		FlowerMesh.VertexColor = Vector3.new((math.random(1, 3) * 0.1), 7.8, (math.random(1, 3) * 0.1))
		FlowerMesh.Scale = Vector3.new(1, 3, 1)
	elseif PlantType == 2 then
		GrowHeight = (1.25 + (math.random() * 0.5))
		local Colors = {
			Vector3.new(0, 0.5, 1), --Blue
			Vector3.new(1, 1, 0), --Yellow
			Vector3.new(0.5, 0.5, 1), --Purple
			Vector3.new(1, 1, 1), --White
		}
		FlowerMesh.MeshId = (BaseUrl .. "111797211")
		FlowerMesh.TextureId = (BaseUrl .. "225802148")
		FlowerMesh.VertexColor = Colors[math.random(1, #Colors)]
		FlowerMesh.Scale = Vector3.new(0.5, 0.5, 0.5)
	end
	FlowerMesh.Parent = Flower
	local Weld = Instance.new("Motor")
	Weld.Part0 = Part
	Weld.Part1 = Flower
	Weld.C0 = ToObjectSpace
	Weld.C1 = CFrame.new()
	Weld.Parent = Flower
	Sounds.Flowers:Play()
	Spawn(function()
		local GrowFrames = math.ceil(5 / GrowRate)
		for i = 1, GrowFrames do
			if Weld and Weld.Parent then
				wait(GrowRate)
				Weld.C1 = CFrame.new(0, (-(i / GrowFrames) * GrowHeight),0)
			end
		end
	end)
	return Flower
end

function CheckIfAlive()
	return (((Player and Player.Parent and Character and Character.Parent and Humanoid and Humanoid.Parent and Humanoid.Health > 0 and Torso and Torso.Parent) and true) or false)
end

function Equipped()
	Character = Tool.Parent
	Player = Players:GetPlayerFromCharacter(Character)
	Humanoid = Character:FindFirstChild("Humanoid")
	Torso = Character:FindFirstChild("Torso")
	if not CheckIfAlive() then
		return
	end
	Spawn(function()
		InvokeClient("PlayAnimation", Animations.Equip)
	end)
	Sounds.Unsheath:Play()
	ToolEquipped = true
end

function Unequipped()
	Attacking = false
	FlameDuration = 0
	FlamesActive = false
	for i, v in pairs(Sounds) do
		v:Stop()
	end
	if CheckIfAlive() then
		Humanoid.WalkSpeed = 16
	end
	CreateFirePart(false)
	ToolEquipped = false
end

function InvokeClient(Mode, Value)
	local ClientReturn = nil
	pcall(function()
		ClientReturn = ClientControl:InvokeClient(Player, Mode, Value)
	end)
	return ClientReturn
end

ServerControl.OnServerInvoke = (function(player, Mode, Value)
	if player ~= Player or not ToolEquipped or not CheckIfAlive() then
		return
	end
	if Mode == "Button1Click" then
		local Down = Value.Down
	elseif Mode == "KeyPress" then
		local Down = Value.Down
		local Key = Value.Key
		local ByteKey = string.byte(Key)
		if Key == "q" and Down and SpecialEnabled and not SpecialActive then
			SpecialEnabled = false
			SpecialActive = true
			Humanoid.WalkSpeed = 0
			InvokeClient("PlayAnimation", Animations.Summon)
			local Radius = 20
			local FlowerModel = Instance.new("Model")
			FlowerModel.Name = "Flowers"
			local FlowerScriptClone = FlowerScript:Clone()
			local Creator = Instance.new("ObjectValue")
			Creator.Name = "Creator"
			Creator.Value = Player
			Creator.Parent = FlowerScriptClone
			local Duration = Instance.new("NumberValue")
			Duration.Name = "Duration"
			Duration.Value = SpecialDuration
			Duration.Parent = FlowerScriptClone
			FlowerScriptClone.Disabled = false
			FlowerScriptClone.Parent = FlowerModel
			FlowerModel.Parent = game:GetService("Workspace")
			for i = 1, 250 do
				if not ToolEquipped or not CheckIfAlive() then
					break
				end
				local Direction = (Torso.CFrame * CFrame.Angles(0, (math.random(-360, 360) * 0.01), 0)).lookVector
				local Distance = (math.random(-(Radius * 1000), (Radius * 1000)) * 0.001)
				local Position = (Torso.CFrame + Direction * Distance).p
				local RayHit, RayPos, RayNormal = CastRay(Position, Vector3.new(0, -1, 0), (Torso.Size.Y * 4), {Character}, false)
				if RayHit then
					local Flower = GrowFlower(RayHit, RayPos)
					if (i % 2) == 1 then
						--local Smoke = Instance.new("Smoke")
						--Smoke.Color = Color3.new((255 / 255), (255 / 255), (0 /255))
						--Smoke.Opacity = 0.15
						--Smoke.RiseVelocity = 0.25
						--Smoke.Size = 0.1
						--Smoke.Enabled = true
						--Smoke.Parent = Flower
					end
					Flower.Parent = FlowerModel
				end
			end
			wait(5)
			if CheckIfAlive() then
				Humanoid.WalkSpeed = 16
				Spawn(function()
					InvokeClient("StopAnimation", Animations.Summon)
				end)
			end
			wait(SpecialDuration - 5)
			SpecialActive = false
			wait(SpecialReloadTime)
			SpecialEnabled = true
		end
	end
end)

CreateFirePart(false)

Handle.Touched:connect(Blow)

Tool.Activated:connect(Activated)
Tool.Equipped:connect(Equipped)
Tool.Unequipped:connect(Unequipped)]]></ProtectedString>
			</Properties>
			<Item class="Script" referent="RBX03A738664CF0491AA942A21D224489DA">
				<Properties>
					<bool name="Disabled">true</bool>
					<Content name="LinkedSource"><null></null></Content>
					<string name="Name">FlowerScript</string>
					<ProtectedString name="Source"><![CDATA[--Made by Luckymaxer

Model = script.Parent
Creator = script:FindFirstChild("Creator")
Duration = script:FindFirstChild("Duration")

Players = game:GetService("Players")
Debris = game:GetService("Debris")

PoisonDamage = 1

Rate = (1 / 60)

function IsTeamMate(Player1, Player2)
	return (Player1 and Player2 and not Player1.Neutral and not Player2.Neutral and Player1.TeamColor == Player2.TeamColor)
end

function TagHumanoid(humanoid, player)
	local Creator_Tag = Instance.new("ObjectValue")
	Creator_Tag.Name = "creator"
	Creator_Tag.Value = player
	Debris:AddItem(Creator_Tag, 2)
	Creator_Tag.Parent = humanoid
end

function UntagHumanoid(humanoid)
	for i, v in pairs(humanoid:GetChildren()) do
		if v:IsA("ObjectValue") and v.Name == "creator" then
			v:Destroy()
		end
	end
end

function CheckTableForInstance(Table, Instance)
	for i, v in pairs(Table) do
		if v == Instance then
			return true
		end
	end
	return false
end

function DestroyModel()
	Debris:AddItem(Model, 1)
end

if not Creator or not Creator.Value or not Creator.Value:IsA("Player") or not Duration then
	DestroyModel()
	return
end

Creator = Creator.Value
Duration = Duration.Value

Character = Creator.Character

if not Character or not Character.Parent then
	DestroyModel()
	return
end

Character.Changed:connect(function()
	if not Character.Parent then
		DestroyModel()
	end
end)

Spawn(function()
	local IgnoreObjects = {Character, Model}
	while Model and Model.Parent do
		local ModelSize = Model:GetModelSize()
		local ModelCFrame = Model:GetModelCFrame()
		local Humanoids = {}
		local NegativePartRadius = Vector3.new((ModelSize.X / 2), (ModelSize.Y / 2), (ModelSize.Z / 2))
		local PositivePartRadius = Vector3.new((ModelSize.X / 2), (ModelSize.Y * 6), (ModelSize.Z / 2))
		local Parts = game:GetService("Workspace"):FindPartsInRegion3WithIgnoreList(Region3.new(ModelCFrame.p - NegativePartRadius, ModelCFrame.p + PositivePartRadius), IgnoreObjects, 100)
		local HumanoidHit = false
		for i, v in pairs(Parts) do
			if v and v.Parent then
				local character = v.Parent
				if character:IsA("Hat") then
					character = character.Parent
				end
				local player = Players:GetPlayerFromCharacter(character)
				if not player or (player and not IsTeamMate(Creator, player)) then
					local humanoid = character:FindFirstChild("Humanoid")
					if not CheckTableForInstance(Humanoids, humanoid) then
					table.insert(Humanoids, humanoid)
						HumanoidHit = true
					end
				end
			end
		end
		for i, v in pairs(Humanoids) do
			if v and v.Parent and v.Health > 0 then
				UntagHumanoid(v)
				TagHumanoid(v, Creator)
				v:TakeDamage(PoisonDamage)
			end
		end
		wait(Rate)
	end
end)

wait(Duration)

DestroyModel()]]></ProtectedString>
				</Properties>
			</Item>
		</Item>
		<Item class="LocalScript" referent="RBX69606FFAD94E460AA28137FA06DF1632">
			<Properties>
				<bool name="Disabled">false</bool>
				<Content name="LinkedSource"><null></null></Content>
				<string name="Name">LocalScript</string>
				<ProtectedString name="Source"><![CDATA[--Made by Luckymaxer

Tool = script.Parent
Handle = Tool:WaitForChild("Handle")

Players = game:GetService("Players")
RunService = game:GetService("RunService")

Camera = game:GetService("Workspace").CurrentCamera

Animations = {}
LocalObjects = {}

ServerControl = Tool:WaitForChild("ServerControl")
ClientControl = Tool:WaitForChild("ClientControl")

ToolEquipped = false

function SetAnimation(mode, value)
	if mode == "PlayAnimation" and value and ToolEquipped and Humanoid then
		for i, v in pairs(Animations) do
			if v.Animation == value.Animation then
				v.AnimationTrack:Stop()
				table.remove(Animations, i)
			end
		end
		local AnimationTrack = Humanoid:LoadAnimation(value.Animation)
		table.insert(Animations, {Animation = value.Animation, AnimationTrack = AnimationTrack})
		AnimationTrack:Play(value.FadeTime, value.Weight, value.Speed)
	elseif mode == "StopAnimation" and value then
		for i, v in pairs(Animations) do
			if v.Animation == value.Animation then
				v.AnimationTrack:Stop()
				table.remove(Animations, i)
			end
		end
	end
end

function DisableJump(Boolean)
	if PreventJump then
		PreventJump:disconnect()
	end
	if Boolean then
		PreventJump = Humanoid.Changed:connect(function(Property)
			if Property ==  "Jump" then
				Humanoid.Jump = false
			end
		end)
	end
end

function CheckIfAlive()
	return (((Character and Character.Parent and Humanoid and Humanoid.Parent and Humanoid.Health > 0 and Player and Player.Parent) and true) or false)
end

function Equipped(Mouse)
	Character = Tool.Parent
	Player = Players:GetPlayerFromCharacter(Character)
	Humanoid = Character:FindFirstChild("Humanoid")
	ToolEquipped = true
	if not CheckIfAlive() then
		return
	end
	PlayerMouse = Player:GetMouse()
	Mouse.Button1Down:connect(function()
		InvokeServer("Button1Click", {Down = true})
	end)
	Mouse.Button1Up:connect(function()
		InvokeServer("Button1Click", {Down = false})
	end)
	Mouse.Button2Down:connect(function()
		InvokeServer("Button2Click", {Down = true})
	end)
	Mouse.Button2Up:connect(function()
		InvokeServer("Button2Click", {Down = false})
	end)
	Mouse.KeyDown:connect(function(Key)
		InvokeServer("KeyPress", {Key = Key, Down = true})
	end)
	Mouse.KeyUp:connect(function(Key)
		InvokeServer("KeyPress", {Key = Key, Down = false})
	end)
	Mouse.Move:connect(function()
		InvokeServer("MouseMove", {Position = Mouse.Hit.p, Target = Mouse.Target})
	end)
end

function Unequipped()
	ToolEquipped = false
	LocalObjects = {}
	for i, v in pairs(Animations) do
		if v and v.AnimationTrack then
			v.AnimationTrack:Stop()
		end
	end
	for i, v in pairs({PreventJump, ObjectLocalTransparencyModifier}) do
		if v then
			v:disconnect()
		end
	end
	Animations = {}
end

function InvokeServer(mode, value)
	local ServerReturn
	pcall(function()
		ServerReturn = ServerControl:InvokeServer(mode, value)
	end)
	return ServerReturn
end

function OnClientInvoke(mode, value)
	if mode == "PlayAnimation" and value and ToolEquipped and Humanoid then
		SetAnimation("PlayAnimation", value)
	elseif mode == "StopAnimation" and value then
		SetAnimation("StopAnimation", value)
	elseif mode == "PlaySound" and value then
		value:Play()
	elseif mode == "StopSound" and value then
		value:Stop()
	elseif mode == "MousePosition" then
		return {Position = PlayerMouse.Hit.p, Target = PlayerMouse.Target}
	elseif mode == "DisableJump" then
		DisableJump(value)
	elseif mode == "SetLocalTransparencyModifier" and value and ToolEquipped then
		pcall(function()
			local ObjectFound = false
			for i, v in pairs(LocalObjects) do
				if v == value then
					ObjectFound = true
				end
			end
			if not ObjectFound then
				table.insert(LocalObjects, value)
				if ObjectLocalTransparencyModifier then
					ObjectLocalTransparencyModifier:disconnect()
				end
				ObjectLocalTransparencyModifier = RunService.RenderStepped:connect(function()
					for i, v in pairs(LocalObjects) do
						if v.Object and v.Object.Parent then
							local CurrentTransparency = v.Object.LocalTransparencyModifier
							if ((not v.AutoUpdate and (CurrentTransparency == 1 or  CurrentTransparency == 0)) or v.AutoUpdate) then
								v.Object.LocalTransparencyModifier = v.Transparency
							end
						else
							table.remove(LocalObjects, i)
						end
					end
				end)
			end
		end)
	end
end

ClientControl.OnClientInvoke = OnClientInvoke
Tool.Equipped:connect(Equipped)
Tool.Unequipped:connect(Unequipped)]]></ProtectedString>
			</Properties>
		</Item>
		<Item class="LocalScript" referent="RBXA41D9588E75C424A807FA678CE13E538">
			<Properties>
				<bool name="Disabled">false</bool>
				<Content name="LinkedSource"><null></null></Content>
				<string name="Name">MouseIcon</string>
				<ProtectedString name="Source"><![CDATA[--Made by Luckymaxer

Mouse_Icon = "rbxasset://textures/GunCursor.png"
Reloading_Icon = "rbxasset://textures/GunWaitCursor.png"

Tool = script.Parent

Mouse = nil

function UpdateIcon()
	if Mouse then
		Mouse.Icon = Tool.Enabled and Mouse_Icon or Reloading_Icon
	end
end

function OnEquipped(ToolMouse)
	Mouse = ToolMouse
	UpdateIcon()
end

function OnChanged(Property)
	if Property == "Enabled" then
		UpdateIcon()
	end
end

Tool.Equipped:connect(OnEquipped)
Tool.Changed:connect(OnChanged)
]]></ProtectedString>
			</Properties>
		</Item>
		<Item class="Animation" referent="RBX0B81C1C0E13344AA948F78F8E7A7CB8E">
			<Properties>
				<Content name="AnimationId"><url>http://www.roblox.com/asset/?id=74894663</url></Content>
				<string name="Name">LeftSlash</string>
			</Properties>
		</Item>
		<Item class="Animation" referent="RBX79741073361C4182AA71ECFA969DA4A3">
			<Properties>
				<Content name="AnimationId"><url>http://www.roblox.com/asset/?id=74813494</url></Content>
				<string name="Name">RightSlash</string>
			</Properties>
		</Item>
		<Item class="Animation" referent="RBX7D57FE7E0C454D208181A269760817F8">
			<Properties>
				<Content name="AnimationId"><url>http://www.roblox.com/asset/?id=225795104</url></Content>
				<string name="Name">Equip</string>
			</Properties>
		</Item>
		<Item class="Animation" referent="RBXC964F6244F814F8D9C9720A924A0C676">
			<Properties>
				<Content name="AnimationId"><url>http://www.roblox.com/asset/?id=93693205</url></Content>
				<string name="Name">Summon</string>
			</Properties>
		</Item>
		<Item class="Camera" referent="RBX713721E1F1614644981E7A8C4A479748">
			<Properties>
				<Ref name="CameraSubject">null</Ref>
				<token name="CameraType">5</token>
				<CoordinateFrame name="CoordinateFrame">
					<X>25.5596027</X>
					<Y>6.95951748</Y>
					<Z>-5.05957603</Z>
					<R00>-0.0624299459</R00>
					<R01>-0.0135210007</R01>
					<R02>0.997957706</R02>
					<R10>-0</R10>
					<R11>0.999908149</R11>
					<R12>0.013547427</R12>
					<R20>-0.998049319</R20>
					<R21>0.000845765113</R21>
					<R22>-0.0624242127</R22>
				</CoordinateFrame>
				<float name="FieldOfView">70</float>
				<CoordinateFrame name="Focus">
					<X>23.5636883</X>
					<Y>6.93242264</Y>
					<Z>-4.93472767</Z>
					<R00>1</R00>
					<R01>0</R01>
					<R02>0</R02>
					<R10>0</R10>
					<R11>1</R11>
					<R12>0</R12>
					<R20>0</R20>
					<R21>0</R21>
					<R22>1</R22>
				</CoordinateFrame>
				<string name="Name">ThumbnailCamera</string>
			</Properties>
		</Item>
	</Item>
</roblox>
<roblox xmlns:xmime="http://www.w3.org/2005/05/xmlmime" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="http://www.roblox.com/roblox.xsd" version="4">
	<External>null</External>
	<External>nil</External>
	<Item class="Tool" referent="RBX0">
		<Properties>
			<bool name="Enabled">true</bool>
			<CoordinateFrame name="Grip">
				<X>0</X>
				<Y>0</Y>
				<Z>-1.89999998</Z>
				<R00>-1</R00>
				<R01>-0</R01>
				<R02>0</R02>
				<R10>0</R10>
				<R11>0</R11>
				<R12>1</R12>
				<R20>-0</R20>
				<R21>1</R21>
				<R22>0</R22>
			</CoordinateFrame>
			<string name="Name">LaserSword</string>
			<Content name="TextureId"><url>http://www.roblox.com/asset/?id=43644997</url></Content>
			<bool name="archivable">true</bool>
		</Properties>
		<Item class="Part" referent="RBX1">
			<Properties>
				<bool name="Anchored">false</bool>
				<float name="BackParamA">-0.5</float>
				<float name="BackParamB">0.5</float>
				<token name="BackSurface">0</token>
				<token name="BackSurfaceInput">0</token>
				<float name="BottomParamA">-0.5</float>
				<float name="BottomParamB">0.5</float>
				<token name="BottomSurface">4</token>
				<token name="BottomSurfaceInput">0</token>
				<int name="BrickColor">107</int>
				<CoordinateFrame name="CFrame">
					<X>8.15499687</X>
					<Y>7.35499334</Y>
					<Z>-26.8300285</Z>
					<R00>1</R00>
					<R01>0</R01>
					<R02>0</R02>
					<R10>0</R10>
					<R11>1</R11>
					<R12>0</R12>
					<R20>0</R20>
					<R21>0</R21>
					<R22>1</R22>
				</CoordinateFrame>
				<bool name="CanCollide">true</bool>
				<bool name="DraggingV1">false</bool>
				<float name="Elasticity">0.5</float>
				<token name="FormFactor">3</token>
				<float name="Friction">0.300000012</float>
				<float name="FrontParamA">-0.5</float>
				<float name="FrontParamB">0.5</float>
				<token name="FrontSurface">0</token>
				<token name="FrontSurfaceInput">0</token>
				<float name="LeftParamA">-0.5</float>
				<float name="LeftParamB">0.5</float>
				<token name="LeftSurface">0</token>
				<token name="LeftSurfaceInput">0</token>
				<bool name="Locked">true</bool>
				<token name="Material">256</token>
				<string name="Name">Handle</string>
				<float name="Reflectance">0</float>
				<float name="RightParamA">-0.5</float>
				<float name="RightParamB">0.5</float>
				<token name="RightSurface">0</token>
				<token name="RightSurfaceInput">0</token>
				<Vector3 name="RotVelocity">
					<X>0</X>
					<Y>0</Y>
					<Z>0</Z>
				</Vector3>
				<float name="TopParamA">-0.5</float>
				<float name="TopParamB">0.5</float>
				<token name="TopSurface">3</token>
				<token name="TopSurfaceInput">0</token>
				<float name="Transparency">0</float>
				<Vector3 name="Velocity">
					<X>0</X>
					<Y>0</Y>
					<Z>0</Z>
				</Vector3>
				<bool name="archivable">true</bool>
				<token name="shape">1</token>
				<Vector3 name="size">
					<X>0.310000151</X>
					<Y>0.310000569</Y>
					<Z>4.82999802</Z>
				</Vector3>
			</Properties>
			<Item class="SpecialMesh" referent="RBX2">
				<Properties>
					<token name="LODX">2</token>
					<token name="LODY">2</token>
					<Content name="MeshId"><url>http://www.roblox.com/asset/?id=43645306</url></Content>
					<token name="MeshType">5</token>
					<string name="Name">Mesh</string>
					<Vector3 name="Offset">
						<X>0</X>
						<Y>0</Y>
						<Z>0</Z>
					</Vector3>
					<Vector3 name="Scale">
						<X>1.5</X>
						<Y>1.5</Y>
						<Z>1.5</Z>
					</Vector3>
					<Content name="TextureId"><url>http://www.roblox.com/asset/?id=43645028</url></Content>
					<Vector3 name="VertexColor">
						<X>1</X>
						<Y>1</Y>
						<Z>1</Z>
					</Vector3>
					<bool name="archivable">true</bool>
				</Properties>
			</Item>
			<Item class="Weld" referent="RBX3">
				<Properties>
					<CoordinateFrame name="C0">
						<X>0</X>
						<Y>0</Y>
						<Z>0</Z>
						<R00>1</R00>
						<R01>0</R01>
						<R02>0</R02>
						<R10>0</R10>
						<R11>1</R11>
						<R12>0</R12>
						<R20>0</R20>
						<R21>0</R21>
						<R22>1</R22>
					</CoordinateFrame>
					<CoordinateFrame name="C1">
						<X>0</X>
						<Y>-1.04904175e-005</Y>
						<Z>0</Z>
						<R00>1</R00>
						<R01>0</R01>
						<R02>0</R02>
						<R10>0</R10>
						<R11>1</R11>
						<R12>0</R12>
						<R20>0</R20>
						<R21>0</R21>
						<R22>1</R22>
					</CoordinateFrame>
					<string name="Name">LaserBladeWeld</string>
					<Ref name="Part0">RBX1</Ref>
					<Ref name="Part1">RBX4</Ref>
					<bool name="archivable">true</bool>
				</Properties>
			</Item>
		</Item>
		<Item class="LocalScript" referent="RBX5">
			<Properties>
				<bool name="Disabled">false</bool>
				<Content name="LinkedSource"><null></null></Content>
				<string name="Name">Local Gui</string>
				<ProtectedString name="Source">local Tool = script.Parent;

enabled = true
function onButton1Down(mouse)
	if not enabled then
		return
	end

	enabled = false
	mouse.Icon = &quot;rbxasset://textures\\GunWaitCursor.png&quot;

	wait(.5)
	mouse.Icon = &quot;rbxasset://textures\\GunCursor.png&quot;
	enabled = true

end

function onEquippedLocal(mouse)

	if mouse == nil then
		print(&quot;Mouse not found&quot;)
		return 
	end

	mouse.Icon = &quot;rbxasset://textures\\GunCursor.png&quot;
	mouse.Button1Down:connect(function() onButton1Down(mouse) end)
end


Tool.Equipped:connect(onEquippedLocal)
</ProtectedString>
				<bool name="archivable">true</bool>
			</Properties>
		</Item>
		<Item class="Script" referent="RBX6">
			<Properties>
				<bool name="Disabled">true</bool>
				<Content name="LinkedSource"><null></null></Content>
				<string name="Name">CrowbarScript</string>
				<ProtectedString name="Source">-------- OMG HAX
-- 43645306:  Full LaserSword
-- 43960328:  LaserBlade
-- 43960368:  LaserHandle

r = game:service(&quot;RunService&quot;)
debris = game:GetService(&quot;Debris&quot;)

local damage = 5
local held_damage = 5
local slash_damage = 10
local spark_color = Color3.new(0, 1, 1)

sword = script.Parent.Handle
Tool = script.Parent


local SlashSound = Instance.new(&quot;Sound&quot;)
SlashSound.SoundId = &quot;http://www.roblox.com/asset/?id=11998777&quot;
SlashSound.Pitch = 1
SlashSound.Volume = 1
SlashSound.Parent = sword


local ClashSound = Instance.new(&quot;Sound&quot;)
ClashSound.SoundId = &quot;http://www.roblox.com/asset/?id=44463749&quot;
ClashSound.Pitch = 1
ClashSound.Volume = .7
ClashSound.Parent = sword


local UnsheathSound = Instance.new(&quot;Sound&quot;)
UnsheathSound.SoundId = &quot;http://www.roblox.com/asset/?id=11998796&quot;
UnsheathSound.Pitch = 1
UnsheathSound.Volume = 1
UnsheathSound.Parent = sword

function waitForChild(parent, childName)
	local child = parent:findFirstChild(childName)
	if child then return child end
	while true do
		child = parent.ChildAdded:wait()
		if child.Name==childName then return child end
	end
end

-- declarations

local Figure = Tool.Parent
local hum = nil

while Figure == nil or Figure.Name == &quot;BackPack&quot; or hum == nil do 
	Figure = Tool.Parent
	if Figure ~= nil then hum = Figure:FindFirstChild(&quot;Humanoid&quot;) end
	wait(1)
end


local Torso = waitForChild(Figure, &quot;Torso&quot;)
local RightShoulder = waitForChild(Torso, &quot;Right Shoulder&quot;)
local initialShoulderFrame = nil
--local LeftShoulder = waitForChild(Torso, &quot;Left Shoulder&quot;)
--local RightHip = waitForChild(Torso, &quot;Right Hip&quot;)
--local LeftHip = waitForChild(Torso, &quot;Left Hip&quot;)
--local Neck = waitForChild(Torso, &quot;Neck&quot;)
--local Humanoid = waitForChild(Figure, &quot;Humanoid&quot;)
--local SwordAngle = waitForChild(sword, &quot;SwordAngle&quot;)
--local SwordC1 = waitForChild(sword, &quot;SwordC1&quot;)

-- ANIMATION
--local downLeftPose = waitForChild(Tool, &quot;downLeftTest&quot;)
--local downRightPose = waitForChild(Tool, &quot;downrightTest&quot;)
local downLeftPose = waitForChild(Tool, &quot;DL&quot;)
local downRightPose = waitForChild(Tool, &quot;DR&quot;)
local upRightPose = waitForChild(Tool, &quot;UR&quot;)
local middlePose = waitForChild(Tool, &quot;M&quot;)
local upLeftPose = waitForChild(Tool, &quot;upleftTest&quot;)

local dL = nil
local dR = nil
local uR = nil
local M = nil
local uL = nil
local prev = nil

print(&quot;Got past the intro!&quot;)


function blow(hit)

	--if (Tool.Enabled) then return end -- only damages on a swing

	if hit == nil or hit.Parent == nil then return end

	local humanoid = hit.Parent:findFirstChild(&quot;Humanoid&quot;)
	local vCharacter = Tool.Parent
	if vCharacter == nil then return end
	local vPlayer = game.Players:playerFromCharacter(vCharacter)
	local hum = vCharacter:findFirstChild(&quot;Humanoid&quot;) -- non-nil if tool held by a character
	if humanoid~=nil and humanoid ~= hum and hum ~= nil then
		-- final check, make sure sword is in-hand

		local right_arm = vCharacter:FindFirstChild(&quot;Right Arm&quot;)
		if (right_arm ~= nil) then
			local joint = right_arm:FindFirstChild(&quot;RightGrip&quot;)
			if (joint ~= nil and (joint.Part0 == sword or joint.Part1 == sword)) then
				tagHumanoid(humanoid, vPlayer)
				humanoid:TakeDamage(damage)
				wait(1)
				untagHumanoid(humanoid)
			end
		end
	elseif humanoid == nil and hum ~= nil then
		if hit.Parent.Name == &quot;LaserSword&quot; then
			humanoid = hit.Parent.Parent
			if humanoid ~= nil then
				humanoid = humanoid:FindFirstChild(&quot;Humanoid&quot;)
				if humanoid ~= nil then
					-- hit an enemy laserSword which is being held by an opponent, so make sure our sword is in-hand
					local right_arm = vCharacter:FindFirstChild(&quot;Right Arm&quot;)
					if (right_arm ~= nil) then
						local joint = right_arm:FindFirstChild(&quot;RightGrip&quot;)
						if (joint ~= nil and (joint.Part0 == sword or joint.Part1 == sword)) then
							if damage &gt; held_damage then
								ClashSound.Pitch = .5 + math.random()*.5
								ClashSound:Play()
							end

							--prev:Stop(.2)
							if prev ~= nil then 
								prev:Stop()
								if M ~= nil and prev ~= M then
									M:Play(.4) 
									prev = M
								end
								damage = held_damage
							end

							-- stop sword&apos;s motion!  produce sparks too! :D
							-- sparks here:
							tempSparks = sword:FindFirstChild(&quot;LaserSwordSparks&quot;)
							if tempSparks == nil then
								tempSparks = Instance.new(&quot;Sparkles&quot;)
								tempSparks.Color = spark_color
								tempSparks.Parent = sword
								debris:AddItem(tempSparks, .5)
							end

						end
					end
				end
			end
		end
	end
end


function tagHumanoid(humanoid, player)
	local creator_tag = Instance.new(&quot;ObjectValue&quot;)
	creator_tag.Value = player
	creator_tag.Name = &quot;creator&quot;
	creator_tag.Parent = humanoid
end

function untagHumanoid(humanoid)
	if humanoid ~= nil then
		local tag = humanoid:findFirstChild(&quot;creator&quot;)
		if tag ~= nil then
			tag.Parent = nil
		end
	end
end

Tool.Enabled = true
local waitCount = 0
function onActivated()
	if not Tool.Enabled then
		return
	end

	Tool.Enabled = false

	local character = Tool.Parent;
	local humanoid = character.Humanoid
	if humanoid == nil then
		print(&quot;Humanoid not found&quot;)
		Tool.Enabled = true
		return 
	end

	local head = character:FindFirstChild(&quot;Head&quot;)
	if head == nil then
		print(&quot;Head not found&quot;)
		Tool.Enabled = true
		return
	end

	local selector = 0
	local targetPos = humanoid.TargetPoint
	local attackVector = head.CFrame:vectorToObjectSpace(targetPos - head.Position)
	if attackVector.y &gt; 0 then selector = selector + 2 end -- ups are later
	if attackVector.x &lt; 0 then selector = selector + 1 end -- rights are odd

	print(attackVector)

	if prev ~= nil then
		if selector == 0 and prev ~= dL then
			prev:Stop()
			dL:Play(.2)
			prev = dL
			waitCount = 2
			damage = slash_damage
			SlashSound.Pitch = .9 + math.random()*.25
			SlashSound:Play()
		elseif selector == 1 and prev ~= dR then
			prev:Stop()
			dR:Play(.2)
			prev = dR
			waitCount = 2
			damage = slash_damage
			SlashSound.Pitch = .9 + math.random()*.25
			SlashSound:Play()
		elseif selector == 2 and prev ~= uL then
			prev:Stop()	
			uL:Play(.2)
			prev = uL
			waitCount = 2
			damage = slash_damage
			SlashSound.Pitch = .9 + math.random()*.25
			SlashSound:Play()
		elseif selector == 3 and prev ~= uR then 
			prev:Stop() 
			uR:Play(.2) 
			prev = uR
			waitCount = 2
			damage = slash_damage
			SlashSound.Pitch = .9 + math.random()*.25
			SlashSound:Play()
		end
	end

	print(selector)

	Tool.Enabled = true
end

function onEquipped()
	damage = held_damage
	Figure = Tool.Parent
	hum = Figure:FindFirstChild(&quot;Humanoid&quot;)
	if hum == nil then
		print(&quot;could not find humanoid!&quot;)
		return
	end

	-- give &apos;em the animations
	dL = hum:LoadAnimation(downLeftPose)
	dR = hum:LoadAnimation(downRightPose)
	uR = hum:LoadAnimation(upRightPose)
	M = hum:LoadAnimation(middlePose)
	uL = hum:LoadAnimation(upLeftPose)

	prev = M

	UnsheathSound.Pitch = .5 + math.random()*.5
	UnsheathSound:Play()

	Tool.Handle.Mesh.MeshId = &quot;http://www.roblox.com/asset/?id=43960368&quot;
	Tool.RisingBlade.Transparency = .5

	Tool.FlashyScript.Disabled = true
	Tool.RisingBlade.Mesh.VertexColor = Vector3.new(1,1,1)

	for i = 1, 15 do
		Tool.RisingBlade.Mesh.Scale = Vector3.new(1.5,1.5,0.1*i)
		Tool.Handle.LaserBladeWeld.C0 = CFrame.new(Vector3.new(0,0,0.1*i - 1.5))
		wait(.05)
		if prev == nil then return end
	end

	if prev == M then
		waitCount = 0
		M:Play(.2)
		SlashSound.Pitch = .5 + math.random()*.5
		SlashSound:Play()
	end

	Tool.FlashyScript.Disabled = false

	--UnsheathSound:play()
end

function onUnequipped()
	damage = held_damage
	if prev ~= nil then prev:Stop() prev = nil end
	if dL ~= nil then dL:Stop() end
	if uL ~= nil then uL:Stop() end
	if uR ~= nil then uR:Stop() end
	if dR ~= nil then dR:Stop() end
	if M ~= nil then M:Stop() end

	ClashSound:Stop()
	UnsheathSound:Stop()
	SlashSound:Stop()

	Tool.Handle.Mesh.MeshId = &quot;http://www.roblox.com/asset/?id=43645306&quot;
end


script.Parent.Activated:connect(onActivated)
script.Parent.Equipped:connect(onEquipped)
script.Parent.Unequipped:connect(onUnequipped)

connection = sword.Touched:connect(blow)

-- reset to M position after a few seconds of inactivity
while true do
	if waitCount &gt; 0 then
		waitCount = waitCount - 1
		if waitCount == 0 and prev ~= M and prev ~= nil then
			SlashSound.Pitch = .5 + math.random()*.5
			SlashSound:Play()

			damage = held_damage
			prev:Stop()
			M:Play(.2)
			prev = M
		end
	end
	wait(2)
end


</ProtectedString>
				<bool name="archivable">true</bool>
			</Properties>
		</Item>
		<Item class="Part" referent="RBX4">
			<Properties>
				<bool name="Anchored">false</bool>
				<float name="BackParamA">-0.5</float>
				<float name="BackParamB">0.5</float>
				<token name="BackSurface">0</token>
				<token name="BackSurfaceInput">0</token>
				<float name="BottomParamA">-0.5</float>
				<float name="BottomParamB">0.5</float>
				<token name="BottomSurface">0</token>
				<token name="BottomSurfaceInput">0</token>
				<int name="BrickColor">107</int>
				<CoordinateFrame name="CFrame">
					<X>8.15499687</X>
					<Y>7.35500383</Y>
					<Z>-26.8300285</Z>
					<R00>1</R00>
					<R01>0</R01>
					<R02>0</R02>
					<R10>0</R10>
					<R11>1</R11>
					<R12>0</R12>
					<R20>0</R20>
					<R21>0</R21>
					<R22>1</R22>
				</CoordinateFrame>
				<bool name="CanCollide">false</bool>
				<bool name="DraggingV1">false</bool>
				<float name="Elasticity">0.5</float>
				<token name="FormFactor">3</token>
				<float name="Friction">0.300000012</float>
				<float name="FrontParamA">-0.5</float>
				<float name="FrontParamB">0.5</float>
				<token name="FrontSurface">0</token>
				<token name="FrontSurfaceInput">0</token>
				<float name="LeftParamA">-0.5</float>
				<float name="LeftParamB">0.5</float>
				<token name="LeftSurface">0</token>
				<token name="LeftSurfaceInput">0</token>
				<bool name="Locked">false</bool>
				<token name="Material">256</token>
				<string name="Name">RisingBlade</string>
				<float name="Reflectance">0</float>
				<float name="RightParamA">-0.5</float>
				<float name="RightParamB">0.5</float>
				<token name="RightSurface">0</token>
				<token name="RightSurfaceInput">0</token>
				<Vector3 name="RotVelocity">
					<X>0</X>
					<Y>0</Y>
					<Z>0</Z>
				</Vector3>
				<float name="TopParamA">-0.5</float>
				<float name="TopParamB">0.5</float>
				<token name="TopSurface">0</token>
				<token name="TopSurfaceInput">0</token>
				<float name="Transparency">1</float>
				<Vector3 name="Velocity">
					<X>0</X>
					<Y>0</Y>
					<Z>0</Z>
				</Vector3>
				<bool name="archivable">true</bool>
				<token name="shape">1</token>
				<Vector3 name="size">
					<X>0.310000151</X>
					<Y>0.310000569</Y>
					<Z>4.82999802</Z>
				</Vector3>
			</Properties>
			<Item class="SpecialMesh" referent="RBX7">
				<Properties>
					<token name="LODX">2</token>
					<token name="LODY">2</token>
					<Content name="MeshId"><url>http://www.roblox.com/asset/?id=43960328</url></Content>
					<token name="MeshType">5</token>
					<string name="Name">Mesh</string>
					<Vector3 name="Offset">
						<X>0</X>
						<Y>0</Y>
						<Z>0</Z>
					</Vector3>
					<Vector3 name="Scale">
						<X>1.5</X>
						<Y>1.5</Y>
						<Z>1.5</Z>
					</Vector3>
					<Content name="TextureId"><url>http://www.roblox.com/asset/?id=43645028</url></Content>
					<Vector3 name="VertexColor">
						<X>1</X>
						<Y>1</Y>
						<Z>1</Z>
					</Vector3>
					<bool name="archivable">true</bool>
				</Properties>
			</Item>
		</Item>
		<Item class="Script" referent="RBX8">
			<Properties>
				<bool name="Disabled">true</bool>
				<Content name="LinkedSource"><null></null></Content>
				<string name="Name">FlashyScript</string>
				<ProtectedString name="Source">local i = 0
local delta = 0
while true do
	if i &lt;= 0 then delta = .1 end
	if i &gt;= 1 then delta = -.1 end
	script.Parent.RisingBlade.Mesh.VertexColor = Vector3.new(1-i,1-i,1)
	wait(.1)
	i = i + delta
end
</ProtectedString>
				<bool name="archivable">true</bool>
			</Properties>
		</Item>
		<Item class="Animation" referent="RBX9">
			<Properties>
				<Content name="AnimationId"><url>http://www.roblox.com/Asset?ID=44418205</url></Content>
				<string name="Name">upleftTest</string>
				<bool name="archivable">true</bool>
			</Properties>
		</Item>
		<Item class="Animation" referent="RBX10">
			<Properties>
				<Content name="AnimationId"><url>http://www.roblox.com/Asset?ID=44448662</url></Content>
				<string name="Name">DL</string>
				<bool name="archivable">true</bool>
			</Properties>
		</Item>
		<Item class="Animation" referent="RBX11">
			<Properties>
				<Content name="AnimationId"><url>http://www.roblox.com/Asset?ID=44448657</url></Content>
				<string name="Name">DR</string>
				<bool name="archivable">true</bool>
			</Properties>
		</Item>
		<Item class="Animation" referent="RBX12">
			<Properties>
				<Content name="AnimationId"><url>http://www.roblox.com/Asset?ID=44448652</url></Content>
				<string name="Name">M</string>
				<bool name="archivable">true</bool>
			</Properties>
		</Item>
		<Item class="Animation" referent="RBX13">
			<Properties>
				<Content name="AnimationId"><url>http://www.roblox.com/Asset?ID=44448649</url></Content>
				<string name="Name">UR</string>
				<bool name="archivable">true</bool>
			</Properties>
		</Item>
		<Item class="LocalScript" referent="RBX14">
			<Properties>
				<bool name="Disabled">false</bool>
				<Content name="LinkedSource"><null></null></Content>
				<string name="Name">LocalCrowbarScript</string>
				<ProtectedString name="Source">-------- OMG HAX
-- 43645306:  Full LaserSword
-- 43960328:  LaserBlade
-- 43960368:  LaserHandle

r = game:service(&quot;RunService&quot;)
debris = game:GetService(&quot;Debris&quot;)

local damage = 5
local held_damage = 5
local slash_damage = 10
local spark_color = Color3.new(0, 1, 1)

sword = script.Parent.Handle
Tool = script.Parent


local SlashSound = Instance.new(&quot;Sound&quot;)
SlashSound.SoundId = &quot;http://www.roblox.com/asset/?id=11998777&quot;
SlashSound.Pitch = 1
SlashSound.Volume = 1
SlashSound.Parent = sword


local ClashSound = Instance.new(&quot;Sound&quot;)
ClashSound.SoundId = &quot;http://www.roblox.com/asset/?id=44463749&quot;
ClashSound.Pitch = 1
ClashSound.Volume = .7
ClashSound.Parent = sword


local UnsheathSound = Instance.new(&quot;Sound&quot;)
UnsheathSound.SoundId = &quot;http://www.roblox.com/asset/?id=11998796&quot;
UnsheathSound.Pitch = 1
UnsheathSound.Volume = 1
UnsheathSound.Parent = sword

function waitForChild(parent, childName)
	local child = parent:findFirstChild(childName)
	if child then return child end
	while true do
		child = parent.ChildAdded:wait()
		if child.Name==childName then return child end
	end
end

-- declarations

local Figure = Tool.Parent
local hum = nil

while Figure == nil or Figure.Name == &quot;BackPack&quot; or hum == nil do 
	Figure = Tool.Parent
	if Figure ~= nil then hum = Figure:FindFirstChild(&quot;Humanoid&quot;) end
	wait(1)
end


local Torso = waitForChild(Figure, &quot;Torso&quot;)
local RightShoulder = waitForChild(Torso, &quot;Right Shoulder&quot;)
local initialShoulderFrame = nil
--local LeftShoulder = waitForChild(Torso, &quot;Left Shoulder&quot;)
--local RightHip = waitForChild(Torso, &quot;Right Hip&quot;)
--local LeftHip = waitForChild(Torso, &quot;Left Hip&quot;)
--local Neck = waitForChild(Torso, &quot;Neck&quot;)
--local Humanoid = waitForChild(Figure, &quot;Humanoid&quot;)
--local SwordAngle = waitForChild(sword, &quot;SwordAngle&quot;)
--local SwordC1 = waitForChild(sword, &quot;SwordC1&quot;)

-- ANIMATION
--local downLeftPose = waitForChild(Tool, &quot;downLeftTest&quot;)
--local downRightPose = waitForChild(Tool, &quot;downrightTest&quot;)
local downLeftPose = waitForChild(Tool, &quot;DL&quot;)
local downRightPose = waitForChild(Tool, &quot;DR&quot;)
local upRightPose = waitForChild(Tool, &quot;UR&quot;)
local middlePose = waitForChild(Tool, &quot;M&quot;)
local upLeftPose = waitForChild(Tool, &quot;upleftTest&quot;)

local dL = nil
local dR = nil
local uR = nil
local M = nil
local uL = nil
local prev = nil

print(&quot;Got past the intro!&quot;)


function blow(hit)

	--if (Tool.Enabled) then return end -- only damages on a swing

	if hit == nil or hit.Parent == nil then return end

	local humanoid = hit.Parent:findFirstChild(&quot;Humanoid&quot;)
	local vCharacter = Tool.Parent
	if vCharacter == nil then return end
	local vPlayer = game.Players:playerFromCharacter(vCharacter)
	local hum = vCharacter:findFirstChild(&quot;Humanoid&quot;) -- non-nil if tool held by a character
	if humanoid~=nil and humanoid ~= hum and hum ~= nil then
		-- final check, make sure sword is in-hand

		local right_arm = vCharacter:FindFirstChild(&quot;Right Arm&quot;)
		if (right_arm ~= nil) then
			local joint = right_arm:FindFirstChild(&quot;RightGrip&quot;)
			if (joint ~= nil and (joint.Part0 == sword or joint.Part1 == sword)) then
				tagHumanoid(humanoid, vPlayer)
				humanoid:TakeDamage(damage)
				wait(1)
				untagHumanoid(humanoid)
			end
		end
	elseif humanoid == nil and hum ~= nil then
		if hit.Parent.Name == &quot;LaserSword&quot; then
			humanoid = hit.Parent.Parent
			if humanoid ~= nil then
				humanoid = humanoid:FindFirstChild(&quot;Humanoid&quot;)
				if humanoid ~= nil then
					-- hit an enemy laserSword which is being held by an opponent, so make sure our sword is in-hand
					local right_arm = vCharacter:FindFirstChild(&quot;Right Arm&quot;)
					if (right_arm ~= nil) then
						local joint = right_arm:FindFirstChild(&quot;RightGrip&quot;)
						if (joint ~= nil and (joint.Part0 == sword or joint.Part1 == sword)) then
							if damage &gt; held_damage then
								ClashSound.Pitch = .5 + math.random()*.5
								ClashSound:Play()
							end

							--prev:Stop(.2)
							if prev ~= nil then 
								prev:Stop()
								if M ~= nil and prev ~= M then
									M:Play(.4) 
									prev = M
								end
								damage = held_damage
							end

							-- stop sword&apos;s motion!  produce sparks too! :D
							-- sparks here:
							tempSparks = sword:FindFirstChild(&quot;LaserSwordSparks&quot;)
							if tempSparks == nil then
								tempSparks = Instance.new(&quot;Sparkles&quot;)
								tempSparks.Color = spark_color
								tempSparks.Parent = sword
								debris:AddItem(tempSparks, .5)
							end

						end
					end
				end
			end
		end
	end
end


function tagHumanoid(humanoid, player)
	local creator_tag = Instance.new(&quot;ObjectValue&quot;)
	creator_tag.Value = player
	creator_tag.Name = &quot;creator&quot;
	creator_tag.Parent = humanoid
end

function untagHumanoid(humanoid)
	if humanoid ~= nil then
		local tag = humanoid:findFirstChild(&quot;creator&quot;)
		if tag ~= nil then
			tag.Parent = nil
		end
	end
end

Tool.Enabled = true
local waitCount = 0
function onActivated()
	if not Tool.Enabled then
		return
	end

	Tool.Enabled = false

	local character = Tool.Parent;
	local humanoid = character.Humanoid
	if humanoid == nil then
		print(&quot;Humanoid not found&quot;)
		Tool.Enabled = true
		return 
	end

	local head = character:FindFirstChild(&quot;Head&quot;)
	if head == nil then
		print(&quot;Head not found&quot;)
		Tool.Enabled = true
		return
	end

	local selector = 0
	local targetPos = humanoid.TargetPoint
	local attackVector = head.CFrame:vectorToObjectSpace(targetPos - head.Position)
	if attackVector.y &gt; 0 then selector = selector + 2 end -- ups are later
	if attackVector.x &lt; 0 then selector = selector + 1 end -- rights are odd

	print(attackVector)

	if prev ~= nil then
		if selector == 0 and prev ~= dL then
			prev:Stop()
			dL:Play(.2)
			prev = dL
			waitCount = 2
			damage = slash_damage
			SlashSound.Pitch = .9 + math.random()*.25
			SlashSound:Play()
		elseif selector == 1 and prev ~= dR then
			prev:Stop()
			dR:Play(.2)
			prev = dR
			waitCount = 2
			damage = slash_damage
			SlashSound.Pitch = .9 + math.random()*.25
			SlashSound:Play()
		elseif selector == 2 and prev ~= uL then
			prev:Stop()	
			uL:Play(.2)
			prev = uL
			waitCount = 2
			damage = slash_damage
			SlashSound.Pitch = .9 + math.random()*.25
			SlashSound:Play()
		elseif selector == 3 and prev ~= uR then 
			prev:Stop() 
			uR:Play(.2) 
			prev = uR
			waitCount = 2
			damage = slash_damage
			SlashSound.Pitch = .9 + math.random()*.25
			SlashSound:Play()
		end
	end

	print(selector)

	Tool.Enabled = true
end

function onEquipped()
	damage = held_damage
	Figure = Tool.Parent
	hum = Figure:FindFirstChild(&quot;Humanoid&quot;)
	if hum == nil then
		print(&quot;could not find humanoid!&quot;)
		return
	end

	-- give &apos;em the animations
	dL = hum:LoadAnimation(downLeftPose)
	dR = hum:LoadAnimation(downRightPose)
	uR = hum:LoadAnimation(upRightPose)
	M = hum:LoadAnimation(middlePose)
	uL = hum:LoadAnimation(upLeftPose)

	prev = M

	UnsheathSound.Pitch = .5 + math.random()*.5
	UnsheathSound:Play()

	Tool.Handle.Mesh.MeshId = &quot;http://www.roblox.com/asset/?id=43960368&quot;
	Tool.RisingBlade.Transparency = .5

	Tool.FlashyScript.Disabled = true
	Tool.RisingBlade.Mesh.VertexColor = Vector3.new(1,1,1)

	for i = 1, 15 do
		Tool.RisingBlade.Mesh.Scale = Vector3.new(1.5,1.5,0.1*i)
		Tool.Handle.LaserBladeWeld.C0 = CFrame.new(Vector3.new(0,0,0.1*i - 1.5))
		wait(.05)
		if prev == nil then return end
	end

	if prev == M then
		waitCount = 0
		M:Play(.2)
		SlashSound.Pitch = .5 + math.random()*.5
		SlashSound:Play()
	end

	Tool.FlashyScript.Disabled = false

	--UnsheathSound:play()
end

function onUnequipped()
	damage = held_damage
	if prev ~= nil then prev:Stop() prev = nil end
	if dL ~= nil then dL:Stop() end
	if uL ~= nil then uL:Stop() end
	if uR ~= nil then uR:Stop() end
	if dR ~= nil then dR:Stop() end
	if M ~= nil then M:Stop() end

	ClashSound:Stop()
	UnsheathSound:Stop()
	SlashSound:Stop()

	Tool.Handle.Mesh.MeshId = &quot;http://www.roblox.com/asset/?id=43645306&quot;
end


script.Parent.Activated:connect(onActivated)
script.Parent.Equipped:connect(onEquipped)
script.Parent.Unequipped:connect(onUnequipped)

connection = sword.Touched:connect(blow)

-- reset to M position after a few seconds of inactivity
while true do
	if waitCount &gt; 0 then
		waitCount = waitCount - 1
		if waitCount == 0 and prev ~= M and prev ~= nil then
			SlashSound.Pitch = .5 + math.random()*.5
			SlashSound:Play()

			damage = held_damage
			prev:Stop()
			M:Play(.2)
			prev = M
		end
	end
	wait(2)
end


</ProtectedString>
				<bool name="archivable">true</bool>
			</Properties>
		</Item>
	</Item>
</roblox>